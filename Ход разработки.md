# Ход разработки

Журнал реализации программного модуля [`wms/`](wms): что сделано, в каком порядке и что осталось. Обновляется вместе с каждым этапом. Требования (FR, NFR, INV) даны по [спецификации](Спецификация%20по%20диплому.md), план — по сокращённому варианту §15.4.

**Состояние на 25 ноября 2026.** Учётное ядро `core` проходит весь путь склада: приёмка → размещение → заказ → резерв → задание → терминал → консолидация → отгрузка. Серверная часть конструктора готова: мастер, валидация и граф склада. Алгоритмы планирования, стенд экспериментов и клиенты (веб-панель, терминал) ещё не начаты.

## План §15.4 и его выполнение

| Нед. | Дорожка | Содержание по плану | Состояние |
| --- | --- | --- | --- |
| 1 | О | Обзор литературы, формализация постановок | Вне кода, см. `PaperWorks/` |
| 2–3 | И | Каркас, TLS и проверка камеры с телефона, `core`: топология, ячейки, профили, SKU, остатки, движения, инварианты | Сделано, кроме TLS в `docker-compose` и прототипа цикла событий |
| 4–5 | И | Конструктор планировки: генерация ячеек и графа, валидация, мастер типовой планировки | Серверная часть сделана; редактор — в веб-панели |
| 6 | Р | Матрица расстояний, реестр стратегий, S-shape, Return, Largest Gap, Combined, jqwik | Не начато |
| 7 | Р | Эталон OR-Tools, 2-opt, ACO, целевые F1 и F2, JMH | Не начато |
| 8 | И | Заказы, аллокация с резервированием, задания, маршрутный лист в PDF | Сделано |
| 9 | Р | Батчинг: FCFS, Savings, Seed, кластеризация | Не начато |
| 10–11 | И | 3D-сцена, режим сравнения, раскраска, перетаскивание товаров; терминал на 4 экрана | Серверная часть терминала сделана; клиенты не начаты |
| 12 | Р | Загрузчик реальных корзин, имитационная модель, прогоны в CSV | Не начато |
| 13 | Р | Полная серия, статистика, графики | Не начато |
| 14 | О | Записка, презентация, демонстрация | Не начато |

Сверх §15.4 сделано то, что сокращённый вариант исключал: приёмка по документу с расхождениями, роли §5 и журнал аудита. Это закрывает шаги сквозного сценария, которые иначе пришлось бы заменять ручными операциями.

## Выполнено

| Дата | Этап | Результат |
| --- | --- | --- |
| 21.09 | Спецификация, разметка модулей | Структура `wms/` по §12.1, README модулей с покрываемыми FR |
| 24.09 | Каркас сборки | Gradle 9.7, Java 25, Spring Boot 4.1; convention-плагин, каталог версий; библиотеки `layout` и `planner-engine` вынесены из сервисов |
| 26.09 | `shared` | Код ячейки `WH1-R03-12-4-2-K` с контрольным символом Luhn mod 36, разбор QR с префиксами `LOC/SKU/CNT/TSK/WRK` |
| 28.09 | `layout` | Модель планировки (профили, ряды, поворот на 90°), генерация ячеек с координатами `(x, y, z)` |
| 30.09 | `core`: хранение | PostgreSQL 16, Flyway, JPA в режиме `validate`, ошибки RFC 9457, интеграционные тесты на Testcontainers |
| 03.10 | `core.admin` | Пользователи и роли §5, JWT с ротацией токена обновления, bcrypt, журнал аудита |
| 07.10 | `core.topology`, `core.layout` | Склады, зоны с политикой хранения, ячейки; версии планировки в JSONB, перенос ячеек в учёт пакетным upsert |
| 10.10 | `core.catalog` | SKU, штрихкоды с проверкой EAN-13, распознавание кода, классы хранения ячеек |
| 14.10 | `core.inventory` | Остатки, неизменяемый журнал движений (триггер), единая точка проводок, сверка с журналом (INV-04) |
| 17.10 | `core.receiving` | Документ прихода, приёмка с расхождениями, рекомендация ячейки, размещение двумя сканами |
| 21.10 | `core.order` | Заказы, импорт CSV, обеспеченность, волны по дедлайну и приоритету |
| 24.10 | `core.allocation` | Резервы: `@Version` с повтором для заказа, `PESSIMISTIC_WRITE` для волны, срок жизни, сверка INV-03 |
| 28.10 | `core.task` | Тара, сборщики, ручное формирование задания с обходом по адресам |
| 01.11 | Терминал, серверная часть | Вход по бейджу и PIN, смена, выдача заданий `SKIP LOCKED`, подтверждение сканом товара, исключения §6.4, идемпотентность событий |
| 04.11 | `core.shipping` | Консолидация тары, отгрузка со списанием `SHIP`, недопоставка, сверка INV-09 |
| 06.11 | OpenAPI | Описание API по OpenAPI 3.1 и Swagger UI (NFR-M-07) |
| 08.11 | CI | GitHub Actions: сборка и все тесты на каждый пуш |
| 09.11 | Журнал | Этот файл |
| 11.11 | `layout.wizard` | Мастер типовой прямоугольной планировки с коэффициентом формы `k = L/d`; пять типовых профилей стеллажей (FR-M15-03d) |
| 14.11 | `layout.graph` | Граф склада по §8.7: точки отбора с объединением стен, сетка отметок, пересечения, рёбра вдоль осей; проходимость по факту. 21,6 тыс. ячеек → 721 узел за 30–60 мс |
| 17.11 | `layout.validation` | Валидация планировки: границы, пересечения, ширина прохода перед лицевой стороной, депо, связность по графу; замечания с местом на плане |
| 19.11 | `core.layout`: мастер, проверка, граф | API мастера и типовых профилей; проверка с предпросмотром исчезающих ячеек с товаром; сохранение планировки с ошибками отвергается (422 со списком замечаний); граф текущей версии с кэшем по версии |
| 22.11 | Заполнение склада | Массовое заполнение по правилу `RANDOM` (с зерном) и `ABC` (ближе к депо по графу и к «золотой зоне»); ручная установка классов ABC/XYZ; расстояния от одной вершины в графе |
| 25.11 | `core.print` | QR-этикетки ячеек, товаров, тары и бейджей листами A4 3 × 8 (FR-M1-12); маршрутный лист задания с QR `TSK:` (FR-M11-03); кириллица через встроенный DejaVu Sans; тесты распознают каждый QR с отрендеренной страницы |

### Что держит целостность учёта

| Инвариант | Механизм |
| --- | --- |
| INV-01, INV-02 — нет отрицательных остатков и резерва больше остатка | `CHECK` на таблице остатков |
| INV-03 — резервы сходятся с остатками | Сверка `GET /api/v1/allocations/integrity` |
| INV-04 — остаток восстановим из журнала | Все изменения через `StockLedger`; журнал только дополняется (триггер); сверка `GET /api/v1/inventory/integrity` |
| INV-05 — шаги задания нумеруются без повторов | Уникальный индекс |
| INV-06 — не отобрать больше заказанного | `CHECK` на строке заказа |
| INV-07 — без активных резервов при отгрузке | Проверка при отгрузке |
| INV-08 — выделенное место: SKU в одной ячейке зоны | Проверка при размещении и при переводе рядов в зону |
| INV-09 — собранное, но не отгруженное лежит в таре и зоне отгрузки | Отбор и консолидация только движениями; ручные операции с этими местами запрещены; сверка `GET /api/v1/shipping/integrity` |

### Метрики

| | |
| --- | --- |
| Тесты | 155: `shared` — 13, `layout` — 35 (из них 5 свойств jqwik на случайных планировках), `core` — 107 (интеграционные на PostgreSQL, включая конкурентные сценарии) |
| Миграции БД | 9 |
| Код `core` | ≈ 9,2 тыс. строк, тесты ≈ 2,5 тыс. строк |

## Предстоит

Порядок: сначала остаток инженерной дорожки, без которого нечего показывать и не на чем гонять алгоритмы, затем алгоритмы.

### Инженерная часть

| Что | Требования | Приоритет |
| --- | --- | --- |
| Классификатор топологии | FR-M15-08 | S |
| Корреляционное правило заполнения | FR-M15-16, FR-M14-11 | M — вместе со слоттингом |
| Docker-образы, `docker compose up` с TLS для камеры телефона | NFR-M-05, NFR-SEC-05, NFR-SEC-07 | M |
| Конфигурация планировщика с версиями | FR-M13-03, FR-M13-05 | M |
| Вызов планировщика из `core`, ручной режим при его отказе | §12.4, NFR-R-05 | M |
| Остальные печатные формы и выгрузка в XLSX | FR-M11-03, FR-M11-04 | M |
| Монитор диспетчера по WebSocket | FR-M9-07 | S |
| Возврат собранного при отмене заказа после сборки | FR-M10-12 | S |

### Исследовательская часть

| Что | Требования |
| --- | --- |
| Матрица расстояний с кэшем по версии планировки | FR-M8-02, §8.7 Д4 |
| Реестр стратегий; эвристики маршрутизации; property-based тесты | FR-M8-03, NFR-M-03, §9.1 |
| Эталон оптимума, 2-opt, ACO, целевые F1 и F2 | FR-M8-04, FR-M14-06, §8.1 |
| Батчинг | FR-M7-01 .. 07, §9.2 |
| Стратегии аллокации и диспетчеризации | FR-M6-02, FR-M6-06, §9.3 |
| Стенд: генератор заказов, загрузчик корзин, имитационная модель, прогоны, JMH | FR-M14-01 .. 11 |
| ABC/XYZ, совместная встречаемость, корреляционное размещение | FR-M12-01 .. 04 |
| Статистика и графики (Python) | FR-M14-07 |

### Клиенты

| Что | Требования |
| --- | --- |
| Веб-панель: конструктор 2D, 3D-сцена, режим сравнения маршрутов, монитор | FR-M15-01 .. 31 |
| PWA-терминал: сканирование камерой, шаги маршрута, исключения | FR-M10-01 .. 10 |

### Сознательно отложено

Партии и сроки годности (FR-M4-10), циклическая инвентаризация (FR-M4-07, 08), кросс-докинг (FR-M3-07), offline-режим терминала (FR-M10-07), контрольная проверка при упаковке (FR-M11-02) — урезаны в §3.2. Альтернативные единицы измерения (FR-M2-06) и выгрузка во внешнюю систему (FR-M11-07) имеют приоритет C.
