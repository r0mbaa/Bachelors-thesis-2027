package io.github.r0mbaa.wms.core.layout;

/**
 * Сохранена новая версия планировки, ячейки уже перенесены в учёт. Публикуется внутри
 * транзакции сохранения: слушатель может отменить сохранение исключением. Так учёт остатков
 * запрещает убрать из планировки ячейку с товаром (FR-M15-03c), а пакет планировки при этом
 * не зависит от пакета остатков.
 */
public record LayoutSaved(long warehouseId, long version) {
}
